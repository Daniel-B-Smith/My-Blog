This directory contains code for the following post:
http://partiallattice.wordpress.com/2013/02/28/when-in-doubt-use-cython

To compile the two modules, use the commands:

python setup.py build_ext -i
python csetup.py build_ext -i

The two modules will be named fib (for Cython) and _fib (for C).